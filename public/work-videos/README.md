Homepage work clip MP4s are hosted in the `wallprint-pro-assets` Cloudflare R2 bucket.

`WorkVideosSection` references browser-friendly MP4 files from the public R2 development URL:

`https://pub-6ee5a28089da498fa3f4d2d028592f3c.r2.dev/work-videos/`

The JPG posters in this folder are generated static assets used for initial video previews.

For production, prefer attaching a branded/custom R2 domain instead of relying on the `r2.dev` development URL.
